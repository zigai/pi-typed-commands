/** Empty option marker used by select-style argument forms for unset optional values. */
export const UNSET_OPTION = "";

/** Validation message options that render argument names as form field labels. */
export const FORM_MESSAGE_OPTIONS = { nameStyle: "field" } as const;
