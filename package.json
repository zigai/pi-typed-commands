{
  "name": "pi-typed-commands",
  "version": "0.1.0",
  "description": "Library for typed Pi extension slash-command arguments, completions, help, and forms.",
  "keywords": [
    "commands",
    "pi",
    "pi-coding-agent",
    "pi-extension",
    "pi-package",
    "slash-commands",
    "typed-args"
  ],
  "homepage": "https://github.com/zigai/pi-typed-commands#readme",
  "bugs": {
    "url": "https://github.com/zigai/pi-typed-commands/issues"
  },
  "license": "MIT",
  "author": "zigai",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zigai/pi-typed-commands.git"
  },
  "files": [
    "src",
    "schemas",
    "docs",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./core": "./src/core/index.ts",
    "./pi": "./src/pi/index.ts",
    "./skills": "./src/skills/index.ts",
    "./pi-tui": "./src/pi-tui/index.ts",
    "./schema": "./schemas/skill-arguments.schema.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "lint": "oxlint -c ./.oxlintrc.json .",
    "lint:fix": "oxlint -c ./.oxlintrc.json . --fix",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm test",
    "verify": "npm run check",
    "release": "release --root .",
    "release:dry-run": "release --dry --root ."
  },
  "dependencies": {
    "typebox": "^1.3.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.7",
    "@earendil-works/pi-tui": "^0.79.7",
    "@types/node": "^24.10.1",
    "fast-check": "^4.8.0",
    "oxfmt": "^0.44.0",
    "oxlint": "^1.59.0",
    "oxlint-tsgolint": "^0.23.0",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "engines": {
    "node": ">=22.19.0 <27"
  },
  "packageManager": "npm@11.0.0"
}
